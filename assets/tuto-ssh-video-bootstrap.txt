# installer vim (sur les 2 machines)
sudo apt install vim

# Docs ( vim )
  -https://doc.ubuntu-fr.org/vim

(
  appuyer sur 'i' pour faire une insertion (inséré du texte)
  pour sauvegarder et/ou quitter:
     - echap
     - :wq
        - : pour taper une commande dans vim
        - w pour sauvegarder
        - q pour quitteg


# Connaitre l'ip d'une machine (depuis la machine distante)
ifconfig
(recuperer l'address inet)

# installer openssh
sudo apt install openssh-client openssh-server

# demarrer le ssh sur la machine distante
sudo systemctl start ssh.socket

# Se connecter à la machine distante (fingerprint)
ssh user@192.168.1.X
( repondre 'yes' pour confirmer la liaison entre les 2 machines )

cd ~/Bureau
mkdir video-bootstrap

# Envoyer une video (depuis ta machine)
scp -r (-p pour le port) ./fichieràenvoyer user@192.168.1.X:Bureau/video-bootstrap

# Docs
  - https://doc.ubuntu-fr.org/ssh

# Permet de supprimer une clé ssh déjà existant sur l'ip correspondant.
 ssh-keygen -f "/home/USER/.ssh/known_hosts" -R "IP"

sudo mv /home/arinfo3/axel/wordpress.conf /etc/nginx/sites-enabled

// identifier les process en cours sur la machine
sudo nestat -lntp

// ferme le process allumer -9 indique la force qu'il va le kill
sudo kill <PID> -9

pm2 start <monAPP>



sudo adduser <USER>

sudo adduser <USER> sudo 

sudo deluser <USER>


conf nginx for node


server {
        listen 5000;
        listen [::]:5000;

        root /home/melvin/html_sass_bootstrap;

        server_name melvin.site;

        location / {
                proxy_pass http://localhost:5000;
                proxy_http_version 1.1;
                proxy_set_header Upgrade $http_upgrade;
                proxy_set_header Connection 'upgrade';
                proxy_set_header Host $host;
                proxy_cache_bypass $http_upgrade;
                try_files $uri $uri/ =404;
        }

}


server {
        listen 6001;
        listen [::]:6001;

        # root /home/steven/Projet-Nodejs-Javascript;

        server_name stevend.com;

        location / {
                proxy_pass http://127.0.0.1:6000;
                proxy_http_version 1.1;
                proxy_set_header Upgrade $http_upgrade;
                proxy_set_header Connection 'upgrade';
                proxy_set_header Host $host;
                proxy_cache_bypass $http_upgrade;
                # try_files $uri $uri/ =404;
        }

}






